import { styled } from '@mui/material/styles';

export const CarrerPageContainer = styled('div')({
    width: '100%',
    textAlign: 'center',
    marginTop: '20px',
    backgroundColor: '#ffffff',
    paddingTop: '20px',
    paddingBottom: '20px',
});

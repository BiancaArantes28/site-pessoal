import { styled } from '@mui/material/styles';

export const LinkSocialMediaBox = styled('div')({
    marginTop: '30px',
});

export const LinkSocialMediaItem = styled('a')({
    color: '#ffffff',
    textDecoration: 'none',
    marginRight: '10px',
    cursor: 'pointer',
});

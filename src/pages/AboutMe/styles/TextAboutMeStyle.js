import { styled } from '@mui/material/styles';

export const TextAboutMeContainer = styled('div')({
    backgroundColor: 'rgba(0, 0, 0, 0.5)',
    width: '100%',
    paddingTop: '20px',
    paddingBottom: '20px',
});

export const TextAboutMeContent = styled('div')({
    width: '100%',
    color: '#ffffff',
});
